<div class="ui hidden divider"></div>

<h4 class="ui top attached large header">{{ 'ikuzo_rma.form.title'|trans }}</h4>
<div class="ui attached segment">
    {{ form_errors(form) }}

    <div class="two fields">
        <div class="field">
            {{ form_row(form.rmaEnabled) }}
        </div>
        
        <div class="field">
            {{ form_row(form.rmaEmail) }}
        </div>
    </div>
</div>
